# ─── LOCAL DEV ─────────────────────────────────────────────────────────────────
# Copy to .env.local and fill in real values. Cloud shares the Neon DB and
# Neon Auth project with site/ — copy those values from site/.env.local.
# Never commit .env.local.

# Neon Postgres — pooled connection for runtime
DATABASE_URL='postgresql://USER:PASSWORD@HOST-pooler.REGION.aws.neon.tech/DB?sslmode=require'

# Neon Auth — same project as site/
NEXT_PUBLIC_NEON_AUTH_URL=https://HOST.neonauth.REGION.aws.neon.tech/DB/auth
NEON_AUTH_BASE_URL=https://HOST.neonauth.REGION.aws.neon.tech/DB/auth
NEON_AUTH_COOKIE_SECRET=...

# Resend — invite emails for /team admin UI
RESEND_API_KEY=re_...

# PostHog — EU cloud, project: agorio (shared with site/)
NEXT_PUBLIC_POSTHOG_KEY=phc_...
NEXT_PUBLIC_POSTHOG_UI_HOST=https://eu.posthog.com
